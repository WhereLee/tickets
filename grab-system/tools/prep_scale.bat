@echo off
mysql -u root -proot grab_system -e "DELETE FROM grab_record; DELETE FROM grab_order; DELETE FROM user WHERE id > 1; DELETE FROM activity WHERE id > 2;"
mysql -u root -proot grab_system -e "INSERT INTO user (username, password, phone) SELECT CONCAT('user_', n), '123456', CONCAT('138', LPAD(n, 8, '0')) FROM (SELECT @row := @row + 1 AS n FROM (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) a CROSS JOIN (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) b CROSS JOIN (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) c CROSS JOIN (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5 UNION SELECT 6 UNION SELECT 7 UNION SELECT 8 UNION SELECT 9 UNION SELECT 10) d CROSS JOIN (SELECT @row := 0) r) nums LIMIT 3000;"
mysql -u root -proot grab_system -e "UPDATE activity SET total_stock = 5000, available_stock = 5000, status = 1 WHERE id = 1;"
mysql -u root -proot grab_system -e "SELECT COUNT(*) AS user_count FROM user; SELECT id, title, total_stock, available_stock FROM activity WHERE id = 1;"
